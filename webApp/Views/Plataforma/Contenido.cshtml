@model List<SmartSys.Models.Publicaciones>
@{
    ViewBag.Title = "Contenido";
    var plataformaId = ViewBag.PlataformaId;
}

<div class="max-w-6xl mx-auto p-6">
    <h2 class="text-3xl font-extrabold text-purple-900 dark:text-purple-100 mb-6">Contenido de la Plataforma</h2>

    <a href="@Url.Action("CrearPublicacion", new { id = plataformaId })"
       class="inline-block mb-6 px-6 py-3 bg-purple-600 hover:bg-purple-700 text-white font-semibold rounded-lg shadow-md hover:shadow-lg transition-all duration-300">
        + Nueva Publicación
    </a>

    <div class="overflow-x-auto">
        <table class="min-w-full bg-white dark:bg-gray-800 rounded-xl shadow-lg border border-purple-100 dark:border-purple-800">
            <thead class="bg-purple-100 dark:bg-purple-900 text-purple-900 dark:text-purple-100">
                <tr>
                    <th class="px-6 py-3 text-left text-sm font-semibold uppercase">Título</th>
                    <th class="px-6 py-3 text-left text-sm font-semibold uppercase">Tipo</th>
                    <th class="px-6 py-3 text-left text-sm font-semibold uppercase">Fecha</th>
                    <th class="px-6 py-3 text-left text-sm font-semibold uppercase">Archivo</th>
                    <th class="px-6 py-3 text-left text-sm font-semibold uppercase">Acciones</th>
                </tr>
            </thead>
            <tbody class="divide-y divide-gray-200 dark:divide-gray-700">
                @foreach (var p in Model)
                {
                    <tr class="hover:bg-purple-50 dark:hover:bg-purple-950 transition-colors duration-300">
                        <td class="px-6 py-4 font-medium text-gray-900 dark:text-white">@p.titulo</td>
                        <td class="px-6 py-4 text-gray-700 dark:text-gray-300">@p.tipoPublicacion</td>
                        <td class="px-6 py-4 text-gray-700 dark:text-gray-300">@p.fechaPublicacion</td>
                        <td class="px-6 py-4">
                            @if (p.archivoAdjunto != null)
                            {
                                var base64 = Convert.ToBase64String(p.archivoAdjunto);
                                var mimeType = "data:image/png;base64";
                                <img src="data:@mimeType;base64,@base64" alt="Adjunto" class="max-w-[100px] max-h-[100px] rounded shadow" />
                            }
                            else
                            {
                                <span class="text-gray-500 dark:text-gray-400">Sin archivo</span>
                            }
                        </td>
                        <td class="px-6 py-4 flex gap-2">
                            <a href="@Url.Action("VerPublicacion", new { id = p.idPublicacion })"
                               class="px-3 py-1 bg-blue-500 hover:bg-blue-600 text-white rounded-lg shadow-md hover:shadow-lg transition-all duration-300 text-sm">
                                Ver
                            </a>
                            <a href="@Url.Action("EditarPublicacion", new { id = p.idPublicacion })"
                               class="px-3 py-1 bg-purple-600 hover:bg-purple-700 text-white rounded-lg shadow-md hover:shadow-lg transition-all duration-300 text-sm">
                                Editar
                            </a>
                            <a href="@Url.Action("EliminarPublicacion", new { id = p.idPublicacion, idPlataforma = plataformaId })"
                               class="px-3 py-1 bg-red-500 hover:bg-red-600 text-white rounded-lg shadow-md hover:shadow-lg transition-all duration-300 text-sm">
                                Eliminar
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
