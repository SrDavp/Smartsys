@model SmartSys.Models.Plataforma
@{
    ViewBag.Title = "Crear Plataforma";
}

<h2 class="text-2xl font-bold mb-4">Crear Nueva Plataforma</h2>

@using (Html.BeginForm("CrearPlataforma", "Plataforma", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <div class="form-group">
        @Html.LabelFor(m => m.nombrePlataforma)
        @Html.TextBoxFor(m => m.nombrePlataforma, new { @class = "form-control" })
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.descripcionPlataforma)
        @Html.TextAreaFor(m => m.descripcionPlataforma, new { @class = "form-control" })
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.privacidadPlataforma)
        @Html.DropDownListFor(m => m.privacidadPlataforma, new SelectList(new[] { "Público", "Privado" }), "Seleccionar", new { @class = "form-control" })
    </div>

    <div class="form-group">
        @Html.Label("Icono de Plataforma")
        <input type="file" name="IconoFile" class="form-control" />
    </div>

    <div class="form-group">
        @Html.Label("Fondo de Plataforma")
        <input type="file" name="FondoFile" class="form-control" />
    </div>

    <button type="submit" class="btn btn-primary mt-3">Crear Plataforma</button>
}